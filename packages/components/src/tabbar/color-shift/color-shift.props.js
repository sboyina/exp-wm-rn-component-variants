const COLOR_SHIFT_TABBAR_DEFAULTS = {
    morebuttonlabel: 'more',
};
/** Merge caller props with Color Shift Tabbar defaults. */
export function createColorShiftTabbarProps(partial) {
    return { ...COLOR_SHIFT_TABBAR_DEFAULTS, ...partial };
}
//# sourceMappingURL=color-shift.props.js.map